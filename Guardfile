guard "jekyll-plus", :serve => true do
  watch /^source\/*/
  watch /_config.yml/
  ignore /^build/
  ignore /^source\/.asset-cache/
end

# Setup livereload
guard 'livereload' do
  watch /^source\/*/
end
